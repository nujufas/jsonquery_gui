[workspace]
resolver = "2"
members = [
    "crates/core",
    "crates/query",
    "crates/app",
]

[workspace.package]
version = "0.2.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = "symbols"

[profile.dev.package."*"]
opt-level = 2
