# Play rejects an upload whose versionCode is not higher than every one before
# it, so this only ever goes up. `scripts/bump-version.sh` does it; the
# human-readable versionName always comes from the workspace version in the
# repo's root Cargo.toml.
versionCode=1
