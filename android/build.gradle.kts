// Versions live here and in gradle/wrapper/gradle-wrapper.properties; the
// toolchain image (docker/Dockerfile.build) pins the SDK, NDK and Rust.
plugins {
    id("com.android.application") version "9.4.1" apply false
}
